# Contact Form Project
A demo project to implement Server Actions.

Install Shadcnui
```shell
bunx --bun shadcn@latest init
```
Add All Components
```shell
bunx --bun shadcn@latest add 
```
Click `A` to add all components